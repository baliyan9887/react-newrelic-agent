/**
 * @file commitlint.config.js
 * @description
 * Configuration file for Commitlint, a tool to check if your commit messages meet the conventional commit format.
 * This configuration extends the conventional commit rules to ensure consistent and semantic commit messages.
 */
module.exports = { extends: ['@commitlint/config-conventional'] }
